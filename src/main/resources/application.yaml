spring:
  application:
    name: gitryeokoffice

  datasource:
    url: jdbc:mysql://localhost:3306/gitryeokoffice?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: asd1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

  h2:
    console:
      enabled: true

# JWT 설정
jwt:
  secret: gitryeokoffice-secret-key-for-jwt-token-generation-minimum-256-bits
  expiration: 604800000  # 7일 (밀리초)
