package com.example.gitryeokoffice.tag.domain;

/**
 * 태그 타입
 * 향후 확장 가능성을 고려하여 ENUM으로 관리
 */
public enum TagType {
    TECH_STACK  // 기술 스택 (예: Spring Boot, React, MySQL)
    // 향후 추가 가능: TOPIC, DOMAIN, etc.
}
